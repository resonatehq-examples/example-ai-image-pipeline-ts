{
  "name": "example-ai-image-pipeline-ts",
  "version": "1.0.0",
  "description": "AI image generation pipeline with parallel fan-out -- generate multiple style variations simultaneously, with crash recovery",
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun run src/index.ts",
    "start:crash": "bun run src/index.ts --crash"
  },
  "dependencies": {
    "@resonatehq/sdk": "^0.9.6"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
